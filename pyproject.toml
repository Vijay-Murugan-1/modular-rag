[project]
name = "rag-intern-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "sentence-transformers>=5.6.0",
]
